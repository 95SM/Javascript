변수에 담을수 있는 값
1)원시타입 - 숫자
              -문자
			  -nill
			  -undifined  : 값이 정의되어 있지 않다. 답을 활성하지 않았다(모든 변수의 기본값)
			  -논리값  -true(참),false(거짓)
			             false로 인식하는 경우
						 0, ",nill,undifined
						  
						  
						  true로 인식하는 경우
						  false로 인식하는 경우를 제외한 모든 값
				        
						ll -,&& ->반환값 (논리값X)

(2) 객체

자바스크립트는 
          원시타입 + 객체 -> 전부 담을 수 있다 .